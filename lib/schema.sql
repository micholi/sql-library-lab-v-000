CREATE TABLE series (
  id INTEGER PRIMARY KEY
  
);

CREATE TABLE series (
  id INTEGER PRIMARY KEY
  
);

CREATE TABLE series (
  id INTEGER PRIMARY KEY
  
);

CREATE TABLE series (
  id INTEGER PRIMARY KEY
  
);

CREATE TABLE series (
  id INTEGER PRIMARY KEY
  
);